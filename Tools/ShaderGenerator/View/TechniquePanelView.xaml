<UserControl x:Class="Odyssey.Tools.ShaderGenerator.View.TechniquePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vc="clr-namespace:Odyssey.Tools.ShaderGenerator.View.Converters"
             DataContext="{Binding Compilation, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="72" d:DesignWidth="512" Foreground="Silver">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="Techniques" FontSize="18" Margin="4"/>
        <Border BorderBrush="{DynamicResource Brush04}" BorderThickness="1">
        <ListBox x:Name="techniquePanel"  Height="32" ItemsSource="{Binding Techniques}" PreviewMouseMove="techniquePanel_PreviewMouseMove">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Border>
    </StackPanel>
</UserControl>
